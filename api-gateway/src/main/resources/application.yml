spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: patient-service-route
              uri: http://patient-service:400
              predicates:
                - path=/api/patients/**
              filters:
                - strip-prefix=1


